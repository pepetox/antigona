%h1 Publicaciones
%p#texto-principal
  = getTexto("publicaciones")

%table.table.table-striped
  %thead
    %tr
      %th Hasta el momento hemos publicado
  %tbody
  - @publicacions.each do |pub|
    %tr
      %td= image_tag(pub.img_link, class: "foto_persona")
      %td= link_to pub.name, publicacion_path(pub)
      %td= truncate(pub.description.html_safe, length: 800)
      %td= button_to "Ver Documento", pub.doc_link, class: "btn btn-primary"




